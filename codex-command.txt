Você é um assistente de código (tipo GitHub Copilot / Codex).
Projeto já convertido para Next.js 14 com App Router, tema kraft artesanal e fluxo completo:
- Rotas: `/`, `/montar`, `/sacola`, `/entrega`, `/pagamento`, `/pedido/[id]`
- Componentes em `components/`, estado global em `context/OrderContext.tsx`
- API handlers em `app/api/pedidos` (POST/GET e GET/PATCH por id) usando Prisma
- Tema global em `styles/globals.css` e logo em `public/logo-kraft.svg`
- Prisma configurado em `prisma/schema.prisma` e `lib/prisma.ts`

Próximas evoluções (futuras): integrar Mercado Pago e WhatsApp, painel admin e rastreamento em tempo real.
